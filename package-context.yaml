apiVersion: v1
kind: ConfigMap
metadata:
  name: kptfile.kpt.dev
  labels:
    env: demo
    managed-by: kpt-fn-render
    app: demo
annotations:
  config.kubernetes.io/local-config: "true"
data:
  name: example
