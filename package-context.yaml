apiVersion: v1
kind: ConfigMap
metadata:
  name: kptfile.kpt.dev
  annotations:
    config.kubernetes.io/local-config: "true"
  labels:
    app: demo
    managed-by: kpt-fn-render
data:
  name: example
